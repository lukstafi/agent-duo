# Runtime state files
.peer-sync/*.status
.peer-sync/*.path
.peer-sync/rounds/
.peer-sync/phase
.peer-sync/round
.peer-sync/.lock/

# Keep task file if user adds one
!.peer-sync/task.md

# OS files
.DS_Store

# Editor files
*.swp
*~
